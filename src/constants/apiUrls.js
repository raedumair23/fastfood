const BASE_URL = 'https://free-food-menus-api-two.vercel.app';

const apiUrls = {
  base: BASE_URL,
  burgers: `${BASE_URL}/burgers`,
  pizzas: `${BASE_URL}/pizzas`,
  desserts: `${BASE_URL}/desserts`,
};

export default apiUrls;
